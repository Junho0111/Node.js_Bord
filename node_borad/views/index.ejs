<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>게시판</title>
</head>
<body>
    <h1>2200938소준호의 게시판</h1>
    <a href="/write">새 글 쓰기</a>
    <ul>
        <% posts.forEach(post => { %>
            <li>
                <h3><a href="/post/<%= post.id %>"><%= post.title %></a></h3>
                <p><%= post.content %></p>

                <form action="/delete/<%= post.id %>" method="post" style="display:inline;">
                    <button type="submit">삭제</button>
                </form>
                <a href="/edit/<%= post.id %>">수정</a>
            </li>
        <% }) %>
    </ul>
</body>
</html>
